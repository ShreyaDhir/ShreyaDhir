name: Waka Readme

on:
  schedule:
    # Runs at 12am IST
    - cron: '30 18 * * *'
  workflow_dispatch:
jobs:
  update-readme:
    name: Update Readme with Metrics
    runs-on: ubuntu-latest
    steps:
      - uses: anmol098/waka-readme-stats@V3
        with:
          WAKATIME_API_KEY: ${{ secrets.2ec46af7-96d3-495c-836f-30bf23bb94d2 }}
          GH_TOKEN: ${{ secrets.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 }}
